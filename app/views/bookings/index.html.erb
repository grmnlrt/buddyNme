<h1>Bookings :</h1>
<% @bookings.each do |booking| %>
  <hr>
  <p>Date : <%= booking.date %></p>
  <p>Mission proposée par : <%= booking.mission.user.first_name %></p>
  <p>Accepté par : <%= booking.user.first_name %></p>
  <p><%= booking.user.first_name %> sera rémunéré <%= booking.mission.cost %>$ par <%= booking.mission.user.first_name %></p>
  <hr>
<% end %>
