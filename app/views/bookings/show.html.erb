<div class="container">
  <div class="row">
    <div class="col-xs-12">
    <h1>Thanks for Booking !</h1>
    <p>Here is your booking's information:</p> <br>
      <h4><%= @booking.mission.title %></h4>
      <hr>
      <p>Date : <%= @booking.mission.created_at %></p>
      <p>Mission proposée par : <%= @booking.mission.user.first_name %></p>
      <p>You will get $ <%= @booking.mission.cost %> for this mission <%= link_to "Back to all Missions", missions_path, class: "links_show pull-right" %></p>
      <hr>
    </div>
  </div>
</div>

